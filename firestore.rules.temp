rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES - ALLOW ALL ACCESS FOR MIGRATION
    // TODO: Replace with production rules after migration
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
