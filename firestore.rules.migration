rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES FOR MIGRATION ONLY
    // Allow all read/write operations during migration
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
